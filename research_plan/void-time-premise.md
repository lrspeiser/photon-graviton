# User premise: void time and constant locally measured light speed

Latest clarification, 9 September 2026. The fictional universe may have different time behavior in intergalactic voids. The user intends that behavior itself to stretch light accumulated over long travel, while light goes the same locally measured speed. Do not silently replace this with a model whose physical locally measured speed changes. Current varying optical-factor calculations are candidates with unresolved clock/ruler interpretation, not a settled replacement for the user's premise.

Established clock/ruler bookkeeping, not a new law:

d tau_local = q(x,t) dt,  d ell_local = b(x,t) dx,

d ell_local/d tau_local = c0 implies dx/dt = c0 q/b.

q relates a proposed local physical clock to declared reference time; b relates physical ruler distance to coordinate distance. These must be derived operationally, not selected solely as coordinates. "Same speed" means the local ratio c0 is unchanged. Different coordinate speeds are not automatically violations of that condition. Conversely, declaring an optical phase variable to be physical proper time does not establish that atomic clocks measure it.

A different void clock rate can make a journey take more time on external clocks without increasing the physical distance traveled. If "more ground" denotes an actual increase in physical lengths over time, that introduces spatial dynamics and must be distinguished from the user's nonexpansion requirement. Do not call a physical expanding ruler field merely time slowing. Keep fixed physical spatial geometry as the current baseline unless the user explicitly changes that premise.

Established stationary-propagation limitation: in a time-independent conservative clock/propagation background, conserved reference frequency leads to endpoint clock factors, not a new accumulated redshift proportional to residence time between matched endpoints. Longer delay alone does not demonstrate wave stretching. This conditional mathematical restriction is not a denial of the fictional premise; it tells us what additional dynamics the premise requires.

The physical research target is therefore an evolving void-time field or an explicit history-dependent interaction that produces lasting wave/event stretch, preserves local c0 under the same clock/ruler definitions, and transfers photon energy into companions. No expansion rate is inserted. Static clock-rate contrasts, dynamically evolving clocks, and irreversible/history-dependent transfer must be kept distinct and tested rather than verbally conflated.

The current n model can be matched algebraically to b/q, but this is a kinematic identification, not a matter-action derivation. If b=1 and q=1/n everywhere in a homogeneous example, atomic clock normalization can cancel the reference spectral shift. Spatially varied dynamically evolving fields with matched endpoints are a different case; they must be solved jointly, including clocks, wavelengths, source energy and detector standards. The user has not authorized us to assume that all atoms follow optical phase time.

Continue the full redshift prediction and independent-data program under these constraints. Formula provenance labels remain mandatory. No unique formula or completed physical realization is claimed by this clarification.
