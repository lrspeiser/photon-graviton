# IH-1 release at high acceleration

Declared after the counter-turning fit, before this amendment's calculations.
The best signed joint fit has epsilon=0.756995. Without an additional release
rule, g_total/g_b -> 1-epsilon = 0.243005 as g_b -> infinity, since its positive
term is bounded. That fails recovery of the fixed ordinary-gravity baseline.
Record this adverse asymptotic result, not just its better galaxy/Coma fit.

Multiply the opposite response by
D(g_b)=1/[1+(g_b/g_release)^2].
Then g_total=g_b*(1-epsilon*S*D)+a_sat*f.
Now g_total/g_b -> 1, and all forces remain positive for epsilon<=0.95.
D is a new deterministic detachment postulate, not a measured microscopic rate.
This models loss of the opposite coupling in a large acceleration environment;
it does not establish any actual Solar-System or strong-field observational test.

Refit T at a_sat=3e-9 and k=2, the signed joint winner's fixed choices.
Use g_release={1e-9,1e-8,1e-7} m/s^2 for both galaxy-only and joint objectives:
six fits with three starts each, from the corresponding positive T solution.
Keep bounds, starts, selection criteria and all other data reductions unchanged.
Selection is by validation, before evaluating these new candidates on test/Coma.
All sets were exposed earlier: label this an exposed follow-up, not new evidence.

Verify epsilon=0 restoration and positive total force, plus high-acceleration
limit at a mathematical large-g fixture, independently bounded by
abs(g_total/g_b-1)<=a_sat/g_b+epsilon*(g_release/g_b)^2.
Run the same cluster and primary optical projection refinements.
Keep every earlier formula/result. Publish the improved limit and whether the
observational fit survives; do not declare a fully funded graviton theory.
