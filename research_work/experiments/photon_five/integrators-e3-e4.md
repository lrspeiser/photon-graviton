# E3/E4 integrators declared before execution
E3 uses a radial response fixture on 0<r<4 with Dirichlet endpoints, tau=1, zeta=0.5, ell=0.3. Spatial noise covariance is sqrt(q(r)q(s)) exp(-(r-s)^2/(2*0.15^2)), q=exp(-r^2/0.5^2). White time noise is explicitly a limiting model, not a finite bandwidth photon interaction. Its stationary energy balance is in expectation; signed stochastic energy exchanges do not establish a finite positive photon reservoir. The Lyapunov and independent normal-mode equations must agree. Source subdivision adds covariances, not amplitudes. Orbital mean extra support is fixed to 10% at r=1. Ordinary potential is -1/sqrt(r^2+0.2^2). EMA of a^2 supplies the response. Burn-in is max(10 field times, 5 averaging times) from zero field and average. Measure 20 reference orbits; primary grid64, 1024 steps/orbit, seeds1909/1910/1911, averaging0.1/1/10 orbits. Refine all three averages at seed1909 to grid128 and 2048 steps/orbit. This is a radial fixture, not a Poisson-sourced galaxy; matter backreaction remains unmodeled. Sampling covariance errors are diagnostic and do not replace the exact covariance gate.

E4 uses finite-volume Rusanov Euler fluxes, outflow boundaries x=+-6, CFL0.25, gamma5/3. Initial density is 0.01 plus two Gaussian gas lumps of width0.3 and amplitude1 at +-2, with converging speed0.7 weighted by their density; uniform pressure0.02. Stars follow the declared ballistic tracks. Each emits0.03 energy/time from a common finite fuel account0.5, in width0.2 Gaussian profiles. The field is locally stored (J=0), starts empty, and decays on tau3. Emission and erasure are exact local updates; all decay/erasure returns heat to gas. Run to time6, after passage; save intermediate states. Entropy production is the positive conservative-update residual over an independently advected entropy density; residual outside compression is reported as a numerical-diffusion diagnostic, not identified as physical shock heating. Curvature of the field smoothed by a fixed0.15 Gaussian defines the weak optical diagnostic; a fixed sign Phi=-u means focusing is -u''. Positive curvature maxima on each half-domain are selected without looking at star positions. Record gas density peaks too. The gas-central criterion is |peak position|<0.75. No readout force acts on gas, and prescribed stars do no modeled work; this is an erasure/offset fixture, not a coupled gravity closure. Tests include all eta/grid combinations, source-off, uniform-flow entropy control, and analytic constant-source decay.
