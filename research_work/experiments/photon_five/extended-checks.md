# Additional verification declaration
Declared after white-noise and gas runs, before these additional computations. Keep all original runs and gates.
E3-color: repeat all orbit averaging times, seeds and grid/time refinements with finite temporal correlation0.2 field-time units. Forcing xi follows dxi=-xi dt/tc+B dW/tc; spatial BB^T is unchanged. Both xi and response start at zero. Use the exact joint OU increment and time-integral covariance in the split integrator. Compare independent modal identities with the augmented three-block Lyapunov solve at tc0.05/0.2/1. The mean supplied power is trace Cov(v,xi); damping is trace Cov(v,v). A dynamical finite photon reservoir remains missing. Refined runs use independent discretized realizations, so they compare statistical outcomes rather than pathwise stochastic convergence.

Independent transport controls: 262144 uniformly emitted finite-energy packets with independently sampled survival must agree with analytic stored energy within five sampling standard errors. Every emitted packet is classified as stored or decayed, with no unaccounted energy. For32768 causal packets, no-scattering and constant-scattering mean-square displacement must agree with the independent age-integrated analytic persistent-flight formula within five standard errors. Verify the projected Gaussian point-source curvature readout to1e-4; finite histogram-cell width is included in the diagnostic. Re-run the isolated preexisting CL-F1 optics verification (no CL1/CL2 source loaders). Archive all metrics and the active repository-module inventory; reject imports of cl1/cl2_sources or geometry engines. These are extra numerical checks, not replacement scientific gates.

## Final ray-refinement audit
The original E5 rays changed tolerance but both reached the same imposed maximum step, producing identical final deflections. Preserve that result. Add an explicit maximum-step refinement0.5 to0.125 with the original1e-4 relative gate, before the final report; the source, lens fits and reciprocal-energy runs are unchanged. Rerun the independent audit as a new immutable evidence directory.
