# PF5 implementation declaration
Frozen before executing this implementation, 2026-09-19. The original protocol is intact; an initially truncated terminal display was mistaken for missing text. No protocol gate was changed.

All numerical fixtures use static coordinates. Only adopted distance numbers are read; no expanding distance routine or unseen matter model is called. Data source truncation and uncertain conversion of historical catalogue distances remain limitations.

E1: Sobol seed 1909, nested powers 12/13/14. Disk light is the measured disk surface brightness, linearly interpolated inward with constant central value, truncated at its last positive measured radius. No unmeasured tail is added. Height is Gaussian, with the declared three scale heights. Cluster pressure is interpolated with constant end values to obtain T proportional to P/ne; emissivity is ne^2 sqrt(T). Profiles stop at the measured density edge. Radial sampling uses 2048-point CDF quadrature. Angular flux is softened by 0.05 characteristic radii; repeat the high-resolution scores at 0.025 and 0.1. This is a finite radiation-cell model, not a continuum convergence claim. Zero-distance direction has zero weight. Disk-only light intentionally omits bulges; report this limitation.

E2: independent fixed seeds 1909, 1910, 1911 for the primary; sensitivity seed 1909. Surviving packet ages follow the exact finite-duration exponential distribution. Scatter times are inverted analytically from the age-dependent integrated hazard; directions reset isotropically. Source radius comes from the ordinary Coma gas profile, truncated at 3 Mpc; stellar proxy follows gas density; mixed proxy is 50/50 normalized gas-squared and gas-density emission. Each has the same conditional total luminosity. A projected radial histogram (2048 bins) and a fixed 0.25 Mpc Gaussian readout define the optical response. This Gaussian is an explicit response scale, not a transport fit. Retain signed curvature. The readout itself is nonlocal and not a proof of causal optical microphysics, even though packet transport is causal.
Calibrate one positive coefficient to the old frozen written-response DeltaSigma at 1 Mpc using the low ordinary-mass Coma bracket and primary parameters, seed 1909, 65536 packets. Do this before scoring shear. Use it unchanged for every other run; the upper bracket has a separately stated luminosity scaling equal to its total ordinary-mass ratio. Compare the 5 Mpc contribution with the old frozen response there. Both are conditional optical response quantities. Six exposed Coma measurements have one nonnegative weak-shear geometry nuisance, hence five nominal degrees of freedom; figure uncertainties omit full covariance. The physical radius conversion h=0.7 is an adopted fixed length calibration, not expansion.

E3-E5 detailed integrator choices will be committed before their executions. Failure of a scientific or numerical gate is a recorded outcome, not a reason to alter a threshold.

## E1/E2 refinement declaration after first execution
E1-v1 retained: 175 disks and 12 clusters; median high-resolution score change 0.05109 fails the fixed 0.05 numerical gate. The proposed factor-three activation contrast fails. Increase nested source samples to 16384/32768/65536 with the same seed, profiles, softening and gates; no mechanism retuning.
E2-v1 retained: a programming error passed already-expanded matrices to a kernel that adds its own axes. Execution stopped before shear scoring. Pass one-dimensional radii as required; all physics choices and gates unchanged.
