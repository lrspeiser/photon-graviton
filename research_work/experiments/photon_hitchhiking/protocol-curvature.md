# HH-2: transfer the attached companion's curvature, not its heading

Declared after HH-1 results and before HH-2 calculation. HH-1 carried the
captured heading; none of its81 mean parameter cases passed the paired inward
screen and discrete captures produced scatter and nonarrivals. The owner's
hitchhiking picture can also mean inheriting a companion's turning acceleration.
This is a new law, not a correction of HH-1 or a retuning of its outcomes.

For MS-1's positive-handed equatorial spiral,
t=cos(beta)e_r+sin(beta)e_phi,
C=(t dot grad)t=(sin(beta)/r)[cos(beta)e_phi-sin(beta)e_r].
Thus C has an inward radial component -sin(beta)^2/r even though radial
transport is outward. C is geometric curvature per length, not the ordinary
matter gravitational acceleration. Verify this expression independently by
finite differencing the direction field along its own tangent.

Use HH-1's attachment probability p and same forward capture rate
a=k_on*u*((1+n dot t)/2)^2. Carry a curvature moment q:
p_dot=a(1-p)-k_off*p,
q_dot=a(1-p)*C-k_off*q,
theta_dot=chi*cross(n,q), x_dot=n.
On discrete attachment store C at capture; while attached the ray turns under
chi*(I-n n^T)*C_stored. On release that coupling stops. Outside the emitted
front the ray carries its existing curvature memory until release, without
reading a nonexistent remote field. This is finite memory, not a derived
binding Hamiltonian or an established photon-graviton interaction.
chi is nondimensional in these units; it is not a measured universal constant.

All81 HH-1 parameter tuples, ten impact parameters, and the same three
stochastic representatives are reused for a controlled comparison.
Mean runs dt0.02, the same six fixed cases at dt0.01; event histories at dt0.01
and0.005 with128 realizations per case/impact, seeds190621 and190622.
Keep all outputs and nonarrivals. For the curvature memory, |q|<=2p is the
correct bound because launch radius0.5 bounds |C| by2; do not reuse the
heading moment bound |m|<=p. Angle/position refinement tolerances stay0.01.

The intended comparison is inward paired bending versus sideways shift,
angular blur, arrival losses, residence and timestep sensitivity. A larger
inward bend under this new postulate is only kinematic feasibility. Momentum
reaction, binding energy/dispersion, self-formation of the swirl, a common
stellar acceleration law and an observed cluster shear fit remain missing.

A photon may be represented by a reversible wave-coupling state for this test;
no massive particle is claimed to ride at light speed without an energy cost.
No dark matter, expansion or distance fitting. Existing curvature calculus,
projected steering and two-state rate equations are credited ingredients;
historical novelty is unestablished.
