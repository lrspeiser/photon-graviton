# HH numerical follow-up, declared after the first HH-2 screen

Preserve HH-1 and HH-2 unchanged. This is exposed numerical diagnosis, not an
independent physical validation or parameter fit.

Run the failed HH-1 representative H4-A100-D0.1-K16 at dt 0.005 and 0.0025.
Run all six predeclared HH-2 refinement cases plus the now-exposed largest
eligible inward mean case H2-A100-D0.1-K1 at both timesteps. Use all ten impact
parameters and the same T=80 and observer x=20. Compare observer angle and
position with the original 0.01 absolute tolerance. Report paired
nonarrivals separately: equal censoring is not an accuracy test.
Compare their full terminal state as a diagnostic, without inventing a pass
threshold after seeing it.

Check HH-2 reflection by repeating the seven cases with negative spiral
pitch at dt 0.005. Reflect y, angle and the y component of carried curvature;
the attachment occupation must remain the same. This checks chirality bias
rather than claiming that a one-handed stream is radially symmetric.
Require maximum reflected state discrepancy below 1e-7.

Independently check the discrete event ledger on all four existing event
archives: final captures minus releases equals final bound state, no negative
event counts, observer arrivals have x=20, path increments do not exceed the
declared unit speed times elapsed time. Compare analytic bound-state steering
with the numerical propagator, and exact exponential decay of the carried
moment after the ray has left the emitted front. These are numerical controls,
not a binding energy or matter recoil model.

The largest-bend case is an exposed selection. No novelty, star-rotation fit,
cluster shear validation, or energy-conserving interaction is implied.
