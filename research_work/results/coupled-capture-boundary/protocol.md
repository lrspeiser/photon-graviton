# Boundary dependence of coupled capture

Declared before computation. Reuse the coupled-capture-calibration model, training-only radii/bins, fixed ordinary matter, Wstar=40000(km/s)^2, p=2 and3, and its one-amplitude log10 C scan4..8.5 plus local refinement. Repeat at outer radii30 and200kpc; the previous100kpc result is an exposed control, not new data. These boundaries are sensitivity choices, not inferred galaxy edges or a fitted physical cutoff law.

For each boundary/p, first calculate the profile using the C fitted at100kpc, then recalibrate C using the same coupled evolution and objective. Report both, distinguishing transfer of a parameter from refitting. Keep all12 radial predictions, required mass and potential depth. Do not choose a winning boundary as if it were a first-principles result.

Use the same400-radius/64-angle/multipole24/128-step calibration resolution and800-radius/128-angle/multipole48/256-step confirmation. Require same-grid time and combined refinement differences below0.5km/s at each selected C. Retain failures; refine affected cases without changing the gate. No reserved stars, vertical-force inferences or new astronomical data are read. This does not alter the radiation redshift calculator or solve retention/support and energy supply.
