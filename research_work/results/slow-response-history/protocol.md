# Does a slow response sustain observable stretching?

Declare before computation. Extend the radiation-powered-initiation experiment only by changing Q=(T-M)/tau from tau=1 to tau=10 and100. Keep epsilon=0.01, K=2, sigma=0.5, capture strength10, initial R=0.22 with T=M=D=0, domain0..10 and constant radiation inflow. Retain the same two Gaussian capture regions, transport rules, clocks and t=0..40 history. Reuse the archived tau=1 capture case as an exposed control, not a new test.

Compute 80/160/320-cell versions for each new tau and probes at emission times0.1,5,20. Keep all original conservation, positivity, characteristic-domain, proper-clock identity and 0.003 absolute-z refinement gates. Measure maximum absolute n_t at reference times20,30,40 and max absolute change of n between30 and40. These distinguish response time from persistence of a changing field under continuous through-flow. Numerical near-stationarity is a result over this history, not a universal no-go theorem or a proof about an isotropic universe.

Keep signed temporal work and whole-photon removal as separate energy transfers. Do not fit a different epsilon, well shape or seed for individual probe times. Slowing M's propagation, introducing permanent void memory, or prescribing an evolving source luminosity would be separate physical changes and are not included in this test.

Follow-up after near-stationarity was observed: independently solve the stationary flux ODE at tau=1,10,100 with initial fluxes (F_R,F_T,F_M)=(0.22,0,0). Reconstruct n=1/[1-F_M/(sigma*K)], R=n*F_R,T=n*F_T,M=K(n-1). Equations F_R'=-epsilon*R; F_T'=-Q-Gamma*T+epsilon*R; F_M'=Q. Integrate capture flux Gamma*T and travel time n as additional variables. Check flux conservation to 1e-10 and independently compare predicted endpoint z=1/n_out-1 with every spatial grid's late probe; require 320-cell difference below 1e-5. This checks a computed steady solution against the evolved result, not a theorem that all histories converge to it.
