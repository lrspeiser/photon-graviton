# Mass-preserving trajectory-volume gravity

Before computation: implement unsoftened constant-density tetrahedron gravity through exact planar face integrals and signed solid angles. Validate a unit-mass cube against the preceding independently integrated square-stream benchmark at an interior point, with relative potential/vector-force errors<1e-9. Check a regular tetrahedron's center force and compare external potential/force against independent positive volume quadrature. Numerical agreement does not validate the physical captured-particle postulate.

Prepare ordinary-bar trajectories on source6/source8 grids with the previous law, field L40, initial radial50km/s and T0.25. Store positions and rotating-frame velocities on4097 uniform ages plus0.05 and0.1, allowing reuse across spatial and age refinements. Require Jacobi drift/220^2<1e-5; the matching previous trajectories already underwent separate tolerance checks. Record field hashes and do not treat a partial cache as complete.

The planned source-volume discretization triangulates full-sphere directions and connects age layers as triangular prisms, split consistently into tetrahedra. Every source-age cell carries its assigned positive mass; overlaps add, and no singular/failed cell may silently lose mass. Piecewise-affine trajectory interpolation is a numerical approximation requiring angular and age convergence, not a physical smoothing length. Curvature, source weighting and folded maps must be audited before claiming reliable Galactic forces. Full energy supply, self-gravity, metric/lensing and redshift remain incomplete.

Before volume reconstruction: use all four exact trajectory symmetry partners, a convex-hull angular triangulation, and per-face solid angle times the mean of the three vertex capture factors as the angular source mass. Normalize separately on each launch sphere. This is a new quadrature of the same continuous source law, not the old Gauss weights; report source normalization differences explicitly. Symmetrize the final field over plane reflection and half-turn so arbitrary coplanar-face diagonals cannot break a physical symmetry. Split each source-age prism into three equal-reference-mass tetrahedra with conforming shared-face diagonals. Overlapping/folded tetrahedra add positive mass. Reject degenerate cells rather than dropping their mass.

Initial age layers64/128, followed by256 if needed. Require total normalized mass within1e-12 and age-refinement differences<2 percent potential and5 percent vector acceleration with the prior0.01 floors. Source6/source8 comparisons use the same gates. Mass conservation is necessary but not proof that the piecewise-affine density is converged. No success claim before age and angular refinement; curve interpolation, small-cell roundoff and folds remain limitations to inspect.

Before the extra conditioning check: compare flattened regular tetrahedra at heights0.01,0.0001,0.000001 with exterior32-node positive volume quadrature; require potential and force differences<1e-7. This checks cancellation at thin cells, without asserting stability down to the degeneracy rejection threshold.

Before actual-cell roundoff audit: retain the thinnest scaled-volume cell in each new volume run and compare its five point fields with60-digit evaluation of the same surface formula. Require absolute errors<1e-7 per G total source mass. This tests sampled actual-cell arithmetic, not a global bound for every cell or the geometric interpolation error.

After the retained128/256 source6 late-time force failure, extend age refinement to512 layers with unchanged gates. Source8 will also use512 to permit a matched angular comparison; do not relax the5 percent force criterion.

Before cache interpolation audit: compare full stored Hermite interpolation with every-second-node interpolation at all interval midpoints. Require maximum position change<1e-6kpc and rotating velocity change<0.01km/s. Retain failures. This is an interpolation check relative to resolved cached trajectories, not an independent physical-model or ODE validation.
