# Exposed-sample uncertainty sensitivity

Use the existing coarse-sky out-of-fold constant-rate predictions for all 164 groups. Do not refit a rate, select models or change distances. Summarize residuals (prediction minus observation, expressed as c times redshift difference) in each of the eight predefined sky regions. Compare 10,000 percentile bootstrap replicates resampling individual groups versus whole sky regions, seed 2026090917, reporting global RMS and bias. These are conditional-on-saved-predictions exploratory sampling sensitivities, not validated predictive intervals or independent motion estimates. The row bootstrap is a comparison, not an endorsed independence model. Eight regions are a sensitivity partition, not a proven correlation length.

Separately state the finite calibration-rank requirement of ordinary split conformal prediction, conditional on exchangeable calibration/test units and a model trained separately. For nominal 80%, 90% and 95%, tabulate required order-statistic rank for 3, 7, 8, 19 and 64 calibration units. Use an infinite threshold when the required rank exceeds the available scores. Do not relabel the reused out-of-fold residuals as independent split-conformal calibration scores. Do not transfer uncertainty to ELVES without addressing different distance range, selection, frame and nuisance distributions.
