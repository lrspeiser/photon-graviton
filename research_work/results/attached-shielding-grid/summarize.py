from pathlib import Path
import json
H=Path(__file__).resolve().parent;r=json.loads((H/'results.json').read_text(encoding='utf-8'));assert len(r['rows'])==480
lines=['# Transfer of pair-calibrated shielding across the 3D source grid','','The opacity fitted to the two critical locations is held fixed at '+str(r['kappa_kpc2_per_Msun'])+' kpc^2/Msun. Isotropic straight-ray absorption is calculated at all 240 existing model probes, with boundary 30 kpc and the unchanged baryonic density. No observational holdouts are opened.','','The optional closure is rho_extra=C T(x) rho_b(x), where T is the calculated angle-averaged transmission and C is one common exposure normalization. The opacity is not refitted. For each reported subset we allow its own best C only to establish a generous lower bound on mismatch; these separate constants cannot jointly define one galaxy model. A single whole-grid constant cannot do better on any subset than its subset-specific optimum.','','Known minimax algebra applied to q/T gives the lowest possible worst relative source-density mismatch: (max(q/T)-min(q/T))/(max(q/T)+min(q/T)). This is a model-shape bound, not a velocity residual or confidence level. All source values, transmissions and resolution comparisons are retained.','','| Subset | mu nodes | Points | Best possible worst source-density mismatch |','|---|---:|---:|---:|']
for c in r['summaries']:lines.append(f"| {c['subset']} | {c['mu_nodes']} | {c['n']} | {100*c['minimum_worst_relative_source_error']:.3f}% |")
lines+=['',f"Maximum relative transmission change between the two resolutions is {r['maximum_relative_transmission_refinement']:.6g}. These are development resolutions, not proof of convergence at every location.",'','The favorable two-location ratio was calibrated and does not establish the full shape. Any residual mismatch can reflect the optional constant-opacity/common-history/attached-storage assumptions, or the empirical target itself. It does not exclude all companion capture. Actual energy supply, momentum transfer, storage dynamics, gravity feedback and motion/lensing predictions remain unclosed. All six objectives remain open.','']
(H/'report.md').write_text('\n'.join(lines),encoding='utf-8',newline='\n');print(json.dumps(r['summaries']))
