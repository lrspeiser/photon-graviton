# Independent angular trajectory-interpolation audit

Before calculation: use the existing source6 and source8 cached trajectories and their unchanged ordinary-bar dynamics. For each source8 launch direction, intersect its ray with the source6 angular hull, calculate positive affine triangle weights, and predict the source8 trajectory from the source6 triangle vertices. Compare against the actual cached source8 trajectory at ages0,0.005,0.01,0.025,0.05,0.1,0.25. Include all four symmetry partners and weight errors with normalized source8 Gauss capture weights. Check ray reconstruction, nonnegative weights and unit weight sums. Report initial chord error separately from later evolution error; do not label the finer trajectories an observational holdout.

This directly tests the affine angular approximation used by the volume method. Report RMS and maximum position error, error relative to launch radius, and fraction of source weight with error exceeding0.02 times launch radius. The2 percent scale is a development diagnostic, not a force-error guarantee or observational tolerance. Keep every sample; do not refit weights or discard badly represented trajectories. No new orbit physics or source amplitude.

Use per-parent-cell errors to identify where additional angular trajectories are needed. Any proposed local mesh refinement must preserve all source mass and be evaluated on newly computed trajectories; interpolating more vertices alone is not physical refinement.

After the retained source6-to-source8 interpolation audit: refine every parent spherical triangle by normalized great-circle edge midpoints, yielding four children per parent. This is a nested mesh refinement, not interpolation of the new trajectories. Preserve total solid angle4pi and a fourfold symmetry-completed source representation. Canonicalize directions only within1e-12 for cache matching; store each true direction and its symmetry transform. Reuse existing trajectories where the direction matches and integrate all genuinely new directions with the same L40 ordinary bar and numerical tolerances. Axis trajectories require the exact m=0 polar limit of the even bar expansion, checked against nearby off-axis evaluations; do not perturb the physical launch to evade the axis.

Keep separate1/3kpc sources and the prior physical capture law. Mesh mass weights may be recomputed as a more accurate quadrature of that same law; no fitted amplitudes. After preparation, compare interpolation against the existing source8 trajectories again and recompute volume forces with angular/age gates unchanged. This is a numerical benchmark, not a fresh astronomical holdout or physical validation.

Before nested-volume computation: retain the exact tetrahedral solver, reference source-mass weights, explicit fourfold field symmetry and diagnostic positions. Use the actual nested triangles rather than a fresh unrelated hull triangulation. First compare256/512 age layers separately on each launch sphere, then parent source6 at512 with nested1 at512. Keep2 percent potential and5 percent vector-force gates with0.01 floors. Permit1024 if age checks fail. Report geometric interpolation errors separately from force errors. No pass based only on conserved mass or improved pointwise geometry.

Before nested-trajectory tolerance audit: check indices18,40,70,97 and any exact polar direction on both launch spheres at rtol2e-13 versus the cached2e-11 solution. Require maximum sampled position difference<1e-5kpc, rotating velocity difference<0.05km/s and Jacobi drift/220^2<1e-5. These are selected checks, not independent validation of every new trajectory. Retain failures.

After the strict polar trajectory reaches below the original1e-6kpc cache boundary: retain the original-domain failure. As a separate numerical sensitivity test, continue the axis potential quadratically inside cutoffs1e-4,1e-5,1e-6kpc, matching potential and first derivative at each cutoff. The curvature is read from the same ordinary-bar cache, not fitted to an orbit or data. Compare tighter-tolerance1D polar dynamics (the exact symmetry reduction) with the existing cached axis orbit and across cutoffs. Require position changes<1e-5kpc, velocity changes<0.05km/s and energy drift/220^2<1e-5. Record any poorly converged core curvature separately; agreement of trajectories does not establish a microscopic core law. This numerical continuation is not a new screening force or an assertion that the original cache covers the origin.

The nested256/512 comparison retains one force failure at R3,T0.25,point0.1. Run a1024-layer supplement for that source/epoch, compare512/1024 at all five points, and keep the matched512 angular comparison as its own result. Also audit the retained thinnest cells of new runs with60-digit arithmetic under the prior1e-7 absolute criterion.

After the coverage audit: subdivide every nested1 triangle once more by normalized edge midpoints, preserving the previous vertices and fourfold symmetry. Compute actual new trajectories for both launch spheres with unchanged dynamics and tolerances; reuse hash-verified matching nested1 paths. Keep original mesh/caches and failures intact. Report source-mass normalization, geometry against both previously exposed source8 and targeted interior probes, then age and angular gravity convergence separately. This is whole-sphere numerical refinement, not a parameter fit or an observational holdout.

After the completed second spatial refinement retains 14/30 force failures: compare a six-node quadratic positional map on each first-nested parent triangle with the existing piecewise-affine second mesh, using exactly the same actual cached vertex and midpoint trajectories. Use ray-to-parent barycentric coordinates and standard quadratic triangular shape functions. Check nodal identity and partition of unity; retain every reference and targeted-probe error at all seven ages. Negative shape coefficients describe positions, not negative source masses. Do not calculate gravity by assigning those coefficients as mass weights. No new trajectories, physical coefficients, density convergence or observational validation is implied.

After quadratic interpolation fails the late map: decompose the largest second-refinement force discrepancy (R=3, T=0.1, target (0.1,0,0)) into eight equal source-age intervals at the existing 512 layers, for both nested meshes. Reuse the exact kernel, weights and fourfold symmetry. Each bin retains its fraction of the original total source mass; do not independently normalize bins or remove old material. Require summed bin forces/potentials to reproduce the archived total before interpreting differences. This is a targeted numerical diagnosis, not an independent validation set.

Within the identified age bin, retain per-tetrahedron contributions from the exact kernel, sum the three tetrahedra per source-age prism and apply the existing symmetry average. Aggregate each four-child group into its first-nested parent triangle to compare both meshes on common angular regions. Require totals to reproduce the archived age bin and its 1/8 total mass. Rank vector differences without clipping negative projections or removing directions. Differences include each mesh's existing capture-weight quadrature; they are numerical attribution, not a fitted physical angular source.
