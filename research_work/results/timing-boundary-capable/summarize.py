from pathlib import Path
import json
H=Path(__file__).resolve().parent
r=json.loads((H/'results.json').read_text(encoding='utf-8'));cs=r['cases']
assert len(cs)==16
lines=['# Boundary-capable timing estimator: development refit','','This is a selected-case development study on all 16 invalid cases from the completed frozen simulation batch. No real fluxes or untouched observations are used. Original estimates and coverage remain unchanged.','','## Method and formula provenance','','The population model log W ~ Normal(a + b log(1+z), sigma) is an existing statistical model, not a new physical law. We integrate a piecewise-linear event likelihood against its normal population density using known Gaussian interval integrals. At sigma=0 the integral becomes evaluation at the population mean. The proposed change is numerical/statistical, not a derivation of photon conversion.','','Mean-duration bounds remain 5-100 days and b remains -2 to 3. Scatter now includes zero and retains upper bound 0.6. The normal distribution remains normalized on the original finite width support of 2-256 days; an explicit constraint retains the original maximum outside probability of 1e-6 for every event. No narrower mean/exponent domain is substituted. The acceptance check permits 1e-11 absolute numerical tolerance on that probability; all selected best fits have computed outside mass zero.','','Five SLSQP starts per case are retained, including a zero-scatter start. The best feasible successful start is reported; optimizer success is not proof of global optimality.','','| Case | Original b | Revised b | Revised scatter | Successful starts | Objective spread across successful starts |','|---|---:|---:|---:|---:|---:|']
for c in cs:
 v=c['revised'];q=v['best']['parameters']
 lines.append(f"| {c['label']} | {c['original']['b']:.6f} | {q[1]:.6f} | {q[2]:.6g} | {v['successful_starts']}/5 | {v['successful_objective_range']:.6g} |")
summary=dict(cases=len(cs),maximum_b_change=max(abs(c['original']['b']-c['revised']['best']['parameters'][1]) for c in cs),near_zero_scatter=sum(c['revised']['best']['parameters'][2]<1e-6 for c in cs),maximum_start_objective_spread=max(c['revised']['successful_objective_range'] for c in cs))
lines+=['','## Outcome and remaining work','',f"All 16 cases return a feasible candidate; {summary['near_zero_scatter']} have scatter below 1e-6. Maximum exponent change from the original is {summary['maximum_b_change']:.6g}. The maximum successful-start objective spread is {summary['maximum_start_objective_spread']:.6g}.",'','The prior integration verification passes 35 comparisons to independent quadrature (maximum absolute error 4.45e-16); it establishes only the declared interpolant integral. It does not establish underlying event-grid accuracy, global fitting convergence, or confidence-interval coverage. Small-scatter interpolation corners and optimizer-start differences remain unresolved. Further optimization comparisons and grid checks must precede adopting this method. Then recalibrate all 160 simulations, including fixed-truth profiles, and test new simulations. Do not use this selected sample to claim repaired coverage or redefine the original failed gates.','','No physical conclusion about redshift or companions follows from these synthetic refits. All six scientific goals remain open.','']
(H/'report.md').write_text('\n'.join(lines),encoding='utf-8',newline='\n')
print(json.dumps(summary))
