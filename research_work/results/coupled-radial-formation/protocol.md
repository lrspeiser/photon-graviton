# Coupled moving deposits, gravity and depleted capture

Before computation: start with zero deposits in a fixed ordinary Plummer potential, G=M_b=a=1. Use p6,kappa0=.1,c/v0=1000, capture domain R30, rate amplitudes Cdot=.1 and1 in density per dynamical time. Incoming isotropic B=Cdot/kappa0; attenuate rays using current total depth. Locally merge captured directions into a cold radial massive cohort using beta=|F|/J and rest fraction sqrt(1-beta^2). Move every cohort, including any that leaves the capture domain. No sticking, circularization, deleted escape mass or old deposited background is allowed.

Represent cohorts as spherical thin shells on signed radial trajectories. Use symmetric numerical pair potential -m_i m_j/sqrt(max(r_i,r_j)^2+epsilon^2), with the corresponding self-shell term and force. Epsilon is a numerical regularization, not a proposed physical core. Coarse:64 source shells/4 impact nodes, orbit dt=.002, injection interval.02. Fine:128/8,dt=.001,injection interval.01. Test epsilon=.05 for both and epsilon=.025 at fine resolution. Integrate to T3 and retain T1,2,3. Require coarse/fine total mass within2 percent and enclosed mass fractions at.1,1,3,10 within0.02 absolute. Report the same comparisons for changing epsilon without treating that as numerical resolution at fixed equations.

Track local incident/transmitted/absorbed/rest/bulk-energy accounts, retained total rest mass, and the exact discrete mechanical Hamiltonian. Across injection events, separately accumulate added kinetic energy and gravitational insertion work; require the numerical mechanical residual below0.5 percent of K+|U|. Do not call gravitational insertion work a physically funded energy channel. Track maximum injected beta and the source-opacity variation per light crossing; relativistic or rapid-field cases limit the approximation, not merely the integrator. Preserve all runs and failed gates. No data fitting, unseen observations, collective nonspherical stability, causal radiation evolution or photon-redshift source is supplied.

After the first grid exposed the strong-source breakdown, add descriptive flags recording the first speed above0.03c and opacity logarithm change above0.1 per diameter light crossing. These are approximation warnings, not predeclared observational pass/fail criteria or sharp physical thresholds. Extend only the low-rate core comparison to epsilon=.0125 and.00625 at the existing fine resolution, to assess central-potential sensitivity. Retain the original pair as well.
