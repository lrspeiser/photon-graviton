# Bounded conversion-associated beam-area response

Before fitting: retain alpha, stipulated distances, photon-number conservation and b=1. Postulate D_G^2/D^2=1+eta f, f=1-1/(1+z), eta in [0,10]. The fraction f follows known photon energy bookkeeping E/E_emit=1/(1+z), but the area response is an additional phenomenological postulate, not a derived metric, scattering law or unique mathematical form. It saturates at 1+eta. It must not be described as proven coherent propagation, preserved image sharpness, or an energy-conserving field completion merely because the photon ledger balances.

Reuse the same Pantheon+ standardized magnitude reduction, full covariance, 77 calibrator rows, 466 training rows and 494 farther evaluation rows as opacity-protocol.md. At calibrators use f=1-exp(-alpha D_cal); at higher redshift retain the existing frame convention f=1-1/(1+zHEL), D=ln(1+zHD)/alpha. Recalibrate the common absolute magnitude consistently for each eta. Minimize training GLS score only, then freeze eta for farther predictions. Report point scores on the unchanged propagated calibration covariance, not parameter-marginalized significance. Reproduce eta=0 baseline. Do not change alpha, b, source normalization per object, splits or the response shape after observing results.

Report eta/bounds, training/transfer scores, all predicted magnitudes, redshift-bin residual summaries and implied area changes. Existing observations and prior alternatives are exposed; improvements are exploratory, not blind causal evidence. Source standardization assumptions and the unresolved relation between a beam-area law and a single global spacetime remain explicit. All six goals remain open.
