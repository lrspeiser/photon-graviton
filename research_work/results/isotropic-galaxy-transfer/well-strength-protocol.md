# Well-strength-dependent capture with shared incoming exposure

Before fitting: extend the existing attenuated model by F_g=[g_b(R_disk)/g_ref]^q, g_ref=1000 (km/s)^2/kpc, q in [0,2]. Set k0,g=k0 F_g and C_g=C F_g simultaneously. This preserves C_g/k0,g=C/k0 and therefore a common incoming energy exposure; it does not assign a free deposit amplitude to each galaxy. The choice of g_ref merely fixes units for fitted k0,C. The power response is a new capture postulate using familiar power-law mathematics, not a microphysical derivation or novelty claim. It uses ordinary gravity only, not self-consistent feedback.

Calculate g_b at measured R_disk from the existing gas+fixed stellar templates, linearly interpolating acceleration from zero at the center and using a Kepler continuation if R_disk is beyond the last point. Keep all original galaxies, splits and mass/light assumptions. Fit log10 C,log10 k0,log10 s with the previous bounds plus q in [0,2], using starts (7,-2,0,0),(7,-1,.5,.5),(6,-4,1,1). Objective remains equal-galaxy squared log speed on 89 training galaxies only; freeze for 29/31 evaluation. Compare prior q=0 scores, verifying that the code reproduces that nested case.

Retain the previous resolution check and all provenance limitations. This adds one shared parameter; repeated split use is exploratory. Record whether q hits zero (no preferred stronger-well response) or its upper bound, rather than expanding bounds to get a desired result. No per-galaxy exposure, changed source history or new lensing fit is authorized by this protocol. All six goals remain open.
