# Matched NFW and geometry diagnostic

Before execution: fit stars plus a positive spherical NFW halo to the same six stellar-kinematic systems and exact catalog lens-angle constraint used in lens-profile-compatibility.py. Use identical stellar profile shapes, PSF/apertures, full stellar covariance, mass bounds and constant beta bounds. All stellar bins are fitted; no held-out prediction is claimed.

Two distance choices: the retained regular companion optical branch; and an explicitly alternative flat FLRW comparison with H0=70 km/s/Mpc, Omega_m=0.3, Omega_Lambda=0.7, neglecting radiation. The latter is a benchmark, not an adopted assumption for the hypothetical universe. Angular diameter distances use standard comoving integrals, D_l=chi_l/(1+z_l), D_ls/D_s=1-chi_l/chi_s. Rebuild every physical aperture and light scale under each geometry. The old conditional-geometry.json is static Euclidean and must not be mistaken for standard FLRW.

Known NFW profile: M(<r)=A[ln(1+r/rs)-(r/rs)/(1+r/rs)]. Fit rs/Re in [0.01,100], halo fraction of the required deflection at the catalog angle f_h in [0,1], and beta in [-2,0.45]. Infer positive stellar mass and halo amplitude from the exact-angle constraint, enforcing existing stellar mass bounds. No cosmological concentration prior or critical-density normalization is imposed. The halo amount is target-fitted, unlike the fixed companion inventory; report that distinction.

Use multiple starts and direct Jeans coefficients. Check lens normalization with independent quadrature of the NFW force and analytic projected mass, record boundaries and zero-halo degeneracy, and compare the full-covariance scores to the prior target-fitted companion mixture under its own geometry. Include stars-only exact-lens controls for both geometries. Do not describe imposed lens equality as a predicted success. This test isolates mass-model/geometry choices within a limited spherical dynamics family, not all dark matter or a complete image fit.
