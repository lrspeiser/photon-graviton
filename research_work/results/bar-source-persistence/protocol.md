# Source-weighted central encounters beyond the first turn

Before computation: retain the conditional ordinary bar component, Omega37.5km/s/kpc, radial launch speed50km/s and diagnostic central radius0.1kpc. Launch spheres at1 and3kpc are analyzed separately, not combined into a full volume source. Use thin-absorption angular weights proportional to [W/(W+40000)]^6 with W=-Phi_bar in (km/s)^2. This is an explicit project source hypothesis with prescribed radial velocity; it is not a solved nonspherical radiation field. No deposited self-gravity or other Galactic components.

Angular quadrature: coarse4 Gauss-Legendre mu nodes and8 midpoint azimuths; fine6 and12. Reduce exact z-reflection and pi-rotation symmetries for scalar radius/energy/Lz statistics. Track each orbit to first entry within0.1kpc or T=.25kpc/(km/s), retaining earlier radial turning events. Entry terminates numerical tracking only; do not label material destroyed, captured at the center, or permanently resident there.

For each launch sphere, report source-weighted fractions that have encountered the center in a single birth cohort, and fractions of a continuously injected population whose histories have included such an encounter, at T=.05,.1,.25. The latter is sum w max(T-t_entry,0)/T. It is not the instantaneous central mass fraction. Source remains stationary in the bar frame. Coarse/fine angular differences below0.05 absolute are a declared development convergence gate, not statistical uncertainties.

Integrate at rtol2e-9 and2e-11 with DOP853, max step.0002 and atol=rtol*.01. Require entry classification agreement, terminal-time difference<1e-5, endpoint velocity difference<.05km/s and Jacobi drift/220^2<1e-5. Retain failures and refine affected orbits if needed. Compare fine angular statistics with bar orders40 and64; changes below0.02 absolute provide the field-order gate. No observed or withheld data, source amplitude fit, permanent-retention prescription, full sky mass density or stable-galaxy claim is supplied.

Before the residence extension: for every orbit that enters, continue from its entry state through the remaining time, with no central sink or stopping surface. Record all0.1kpc crossings and integrate the actual time spent inside. Divide weighted inside-age duration by T to obtain the instantaneous central fraction of a continuously injected source. Compare tolerances using inside-duration differences<1e-5 time units and Jacobi drift/220^2<1e-5; preserve any field-domain failures as unresolved. Require angular and field-order differences of this new residence statistic below0.005 absolute as a development gate. This does not validate the entire density profile or deposited self-gravity.

Observed numerical repair: the initial coarse-tolerance residence calculation missed a short passage in fine-angle L40 probe4. Retain all initial files. The corrected audit integrates each complete orbit once, locates both kinds of radial turning point, and brackets0.1kpc crossings on the monotonic-radius intervals between them using the dense solution. This detects a short passage even when entry and exit occur within one solver step. Corrected results use audit-*.json and retain tolerance refinements and raw event-count differences; no acceptance threshold is relaxed.
