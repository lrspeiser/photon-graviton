# Distributed radiation: homogeneous zero-mode protocol

Pre-calculation specification, 9 September 2026. This is an exact spatially homogeneous sector in a finite periodic three-dimensional volume, not a simulation of discrete sources or a proof that a static universe with gravity exists. Geometry is fixed. No expanding scale factor, continuing source, initial rolling field, gravity response or confinement mechanism is introduced.

Set c0=K=g=volume=1, V(phi)=0, n=1+g phi, phi(0)=phi_dot(0)=0. A homogeneous isotropic photon ensemble starts with total energy density U0 in {0,0.003,0.006}. This finite initial supply is counted; its stellar origin is not derived. Canonical photon momenta are conserved in the homogeneous field. E_gamma density=U0/n. E_field density=K n_dot^2/(2g^2). Integrate n_ddot=g^2 U0/(K n^2) to reference time 80. Repeat DOP853 at relative tolerance 1e-9 and 1e-11 (absolute tolerance relative/100).

Compare to the integrated analytic energy relation and implicit time solution. Required numerical energy error below 1e-7 relative to nonzero initial energy; tight/loose n and n_dot differences below 1e-6. Zero-source solution must remain n=1 and n_dot=0.

Propagate independent negligible-energy probes from t_e=1,5,20 over fixed spatial length 2, for canonical momenta 1 and 2. Integrate ray time and carrier log-frequency, then obtain arrival-interval stretching from symmetric launch offsets 1e-4 and 5e-5, without assigning it from the carrier shift. Compare local duration factor with n(t_o)/n(t_e). These are coordinate/reference-clock probes: matter clock coupling is unresolved and must be stated. Do not call this actual observed redshift without endpoint-standard completion.

Retain comparison to the prior localized spherical source as a separate geometry result, not a like-for-like boundary comparison. Report the finite-volume topology assumption, missing inhomogeneous stability, missing local screening and no astronomical units/calibration. No fit to galaxy redshifts occurs.
