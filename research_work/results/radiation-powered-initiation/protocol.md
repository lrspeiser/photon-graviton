# Radiation-powered initiation: declared experiment

No initial T or M seed. Extend the field-funded branch by whole-photon conversion I=epsilon*R into T. R is now explicitly the background photon reference-energy density; GW signals are negligible probes with the same ray law, not a contribution to the seeding source. R_t+div(vR)=-b*M_t-I; T_t+div(vT)=-Q-Gamma*T+I; d*M_t+div(sigma*v*M)=Q; D_t=Gamma*T. Keep n=1+M/K, b=R/(Kn), d=1-b, Q=T-M (response time one), K=2,sigma=0.5, clocks d tau_clock=dt/n. I removes photon number, not frequency from each surviving photon. This is a proposed seeding channel, not a derivation of a graviton interaction.

Use x=0..10, t=0..40, initially R=0.22,T=M=D=0 and constant incoming photon reference-energy flux 0.22 at x=0. No incoming companions/temporal energy. Compare (epsilon,capture strength)=(0,10),(0.01,0),(0.01,10),(0.05,10). Gamma=strength*[exp(-(x/1)^2)+exp(-((x-10)/1)^2)] represents two specified capture regions; it is not a fitted well map or a forced clock reset. These are one-dimensional one-direction transport tests, not an isotropic cosmic radiation calculation. Slower M flux is explicit; photon, GW and T local speed is c0=1.

Use 80,160,320 cells with the existing limited-face method. Track total energy, radiation-out energy, signed temporal work integral b*M_t, and whole-photon conversion integral I separately. Require both total/radiation ledger errors below 1e-8, no energy below -1e-9, d>0, and n*d>sigma on evaluated trajectories. Do not infer global domain invariance from a pass.

For emission times 0.1,5,20, propagate probes from x=0 to10. Compute coordinate event Jacobian, endpoint clock factor, observed z, and photon survival exp[-epsilon*(arrival-emission)] separately. Check event stretching via independently integrated proper-clock intervals for emissions te +/-0.001 to 2e-4; final adjacent-grid absolute-z differences must be below 0.003. Record frequency change and photon loss as distinct predictions. No calibration of epsilon to alpha or data. A separate analytically derived identity, log(1+z)=-integral partial_x(log n) dx along a right-moving ray, explains why homogeneous time changes cancel; do not reinterpret I as that spectral redshift.
