# Chronological capture with deposited gravity

Declared before calculation. Retain p=2 and3, Wstar=40000(km/s)^2, outer radius100kpc and the amplitudes calibrated in milky-way-depth-capture (fine100kpc variants). Do not refit them after adding feedback. Use the same refined ordinary field and only the542-star training-radius cache and12 already exposed Jeans proxies. No validation/test stars or vertical inferences are needed.

Replace the no-feedback approximation rho=C*f(W_b) by chronological growth from zero: d rho/ds=C*[W_total/(W_total+Wstar)]^p for dimensionless exposure age s=0..1, W_total=max[-Phi_b-Phi_D(r,mu,s),0]. Phi_D is the Newtonian potential of the accumulated rho, with zero at infinity and density zero outside100kpc. This is growth history, not an equilibrium fixed-point assignment rho=C*f(W_final). Constant isotropic thin flux, permanent in-place retention and a quasistatic gravitational response remain assumptions. C combines intensity, capture normalization and age; no radiation supply is thereby derived.

Numerics: positive real-space density on400/800 logarithmic radial nodes from1e-4 to100kpc,64/128 Gauss angular nodes, even multipoles through24/48, and64/128 exposure steps. Use explicit midpoint updates (positive source) and a precomputed spherical-harmonic Green kernel with radial trapezoid weights. The kernel is ordinary Poisson mathematics, not new gravity. State the absence of finite-speed formation/support dynamics. Require density0<=rho<=C up to numerical tolerance at every step, because capture factor is between0 and1 and exposure age<=1.

Record mass and solar potential through the history, final radial training predictions/RMS, and final density saturation fraction. Compare the frozen-depth control (rho=C*f(W_b), same kernel) with the prior independently integrated field to within0.5km/s. Require coarse/fine dynamic predictions within2km/s and mass within1 percent; retain any failure and refine if needed. Also rerun the fine spatial grid with64 time steps to isolate time convergence, requiring predictions within2km/s. Numerical convergence is not physical stability: permanent deposits cannot reach a time-independent mass under continuing positive supply.

Potential energy bookkeeping is incomplete; this calculation neither demonstrates the availability of the external flux nor accounts for gravitational work/kinetic support. It tests whether the previously fitted radial curve survives inclusion of the same depth feedback that the model proposes.

Refinement after the p3 time-step difference exceeded2km/s: retain the original failed checks and rerun p3 on400 and800 radial nodes with256 exposure steps. Compare spatial predictions at the same256 steps, and time convergence on800 nodes from128 to256. Keep the same2km/s and1-percent gates and all old runs; no physical parameter or data changes.
