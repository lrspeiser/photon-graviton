# Isotropic arrival and a derived deposition profile

Declare before calculation. Use a spherical capture region radius R=10 in scale-radius units a=1, with isotropic external companion specific intensity I0. Rays travel straight at c0; weak gravitational focusing, scattering, re-emission and capture feedback are omitted explicitly. A fixed baryonic Plummer well sets the capture coefficient kappa(r)=k0/[1+sqrt(1+r*r)] for r<=10, zero outside. This is the previously proposed saturating binding-depth rule specialized to depth proportional to1/sqrt(1+r*r); k0 is not inferred from redshift or a halo fit.

For position r and incoming propagation cosine mu, integrate backwards to the boundary s_b=r*mu+sqrt(R*R-r*r+r*r*mu*mu). The intensity is I0*exp[-integral_0^s_b kappa(sqrt(r*r+s*s-2*r*mu*s))ds]. Derive deposition power density q=kappa*integral I dOmega. Retention in place without escape is an explicit hypothesis, giving rho_D=q*t_age/c0^2; no orbital support or microscopic binding is proved.

Use k0=0.01,0.1,1,10. Compute radial profiles on321 and641 uniformly spaced nodes, with48 and96 Gauss-Legendre nodes respectively for both angle and ray optical-depth integration. Compare total absorbed power to a separate incoming-chord calculation and 4*pi^2*R^2*I0 incident power. Require finest relative disagreement below0.002 and capture fractions within[0,1]. Compare fine/coarse normalized enclosed-mass fractions at r=1,5,9 to absolute0.003.

Compute enclosed mass, potential and projected mass directly from the derived q. Report outer-shell deposited fraction r>5, local q(9)/q(1), and departure from a Plummer enclosed profile on the same finite sphere. Spherical geometry and fixed opacity limit interpretation; this is not a Galactic disk/bulge fit.

For a conditional gravity/clock normalization, set G=c0=a=1, t_age=40, and select intensity so initial central |Phi|=beta=1e-6. The same constant deposition rate then gives Phi(r,t)=(1+t/40)*Phi(r,0), with no separate fitted growth rate. Integrate rays from x=-20 to20 at te=0 with the previous equal-potential diagnostic metric (gamma=1) and proper-clock endpoints. Record required I0, mass, forces and static Born deflection at impact1,5,9; no claim that real radiation supplies this chosen intensity. Check ray stretch against independently integrated proper-clock intervals to1e-7. Initial shape is steady irradiation accumulated over40 units; verify finite traversal time is small relative to illumination age only qualitatively and retain startup/history limitations rather than treating this as a full formation simulation.

History qualification: maximum companion transit is20 units, half the chosen retention age40, so startup cannot be dismissed as negligibly short. The test assumes the irradiating field was established before that retained-deposit interval; it does not compute a switch-on solution. The initial adaptive projected-mass integration raised roundoff warnings at interpolation kinks. Replace it with piecewise Gauss integration on each interpolation segment, transform the outer integral with r=sqrt(b^2+u^2), and compare8 versus16 nodes per segment to relative1e-9. This is a numerical repair, not a changed capture law.
